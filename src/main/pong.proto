syntax = "proto3";
option go_package = "pong.com/main";

package poc;

message Ball {
  int32 x = 1;
  int32 y = 2;
}

message Request {
  string api = 1;
}

service PongService {
  rpc Create(Request) returns (Ball);
}
